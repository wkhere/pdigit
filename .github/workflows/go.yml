name: Go
on: 
  push:
    branches: [ master, exp ]
jobs:

  test:
    runs-on: ubuntu-latest
    steps:

    - uses: actions/checkout@v4
    - uses: actions/setup-go@v5
      with:
        go-version-file: go.mod

    - run: go get -v -d ./...
    - run: go test -v ./...
